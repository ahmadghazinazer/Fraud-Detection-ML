# Node
node_modules/
dist/
.DS_Store

# Python
venv/
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
env/
pip-log.txt
pip-delete-this-directory.txt

# Models
# Note: Usually we do not push large models, but since these are small scikit-learn mock models, we can exclude or include them as needed.
# For now, we will push the models so the repo is plug-and-play.
# backend/models/*.pkl

# Environment
.env
.env.local
